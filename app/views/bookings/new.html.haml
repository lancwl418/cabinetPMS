.wrapper_width_padding
#booking_new
  %h1 All the Houses you in your doc you can assign
  - unless @docs.blank?
    - @docs.each do |doc|
      %a{ href: (url_for [doc])}
        .doc
          %p.id= "ID: #{doc.id}"
          %p.title= doc.title
  - else
    %h2 Create a new house! 
    %button= link_to "Create House", new_doc_path
  %hr
  %h1 All the guests in your doc you can assign

  - unless @guests.blank?
    - @guests.each do |guest|
      %a{ href: (url_for [guest])}
        .guest
          %p.id= " ID: #{guest.id}"
          %p.title= guest.name
  - else
    %h2 Create a new guest! 
    %button= link_to "Create guest", new_guest_path

%p.note ***Please enter the house(Doc) ID and Guest ID you want to assign.
%p.note sample: Doc: 1 Guest: 2 Checkin:01/08/2019 Checkout:02/08/2019

= render 'form'